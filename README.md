# SimpleStaticScanner

git tag -a -m "Initial Commit" v1                                                                                                  

git push --follow-tags

git tag -a -m "Update 2" v2; git push --follow-tags 